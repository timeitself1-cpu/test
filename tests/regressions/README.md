# Regression fixtures

Place only reviewed, synthetic/sanitized replay fixtures in this directory.

Production incident artifacts are metadata-only and are intentionally not replayable. Convert a production incident into a minimal synthetic reproducer before promoting it here.
